// 21 - 10 - 2024

#include <stdio.h>
#include <stdlib.h>

int main(){
    int n =5;
    scanf("%d", &n);
    int* array =(int*) malloc(n * sizeof(int));

// heap memory : in ram a store give memory to be allocated

    return 0;
}